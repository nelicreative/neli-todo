en:
  errors:
    template:
      header:
        one: "1 error prevented %{model} from being saved"
        other: "%{count} errors prevented %{model} from being saved"
    attributes:
      workspace:
        required: "Workspace is required"
    messages:
      required: "required"
  activerecord:
    models:
      task: "Task"
    errors:
      models:
        task:
          attributes:
            workspace:
              required: "Workspace is required"
      messages:
        required: "required"
  tasks:
    new:
      title: "Create New Task"
      label:
        title: "Title"
        description: "Description"
        status: "Status"
        start: "Start"
        end: "Due"
      placeholder:
        title: "Create a title"
        description: "Add a description"
      submit: "Add"
    index:
      title: "Task List"
      noData: "No data found matching your search!!!"
  date:
    order:
      - :day
      - :month
      - :year
  time:
    formats:
      long: "%H:%M %B %d, %Y "
